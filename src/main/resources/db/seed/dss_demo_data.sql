-- DSS demo dataset (historical).
--
-- Điện tử DSS (dss-demo-electronics) + shop seller.dss.demo* đã bị gỡ
-- vì không phục vụ demo thật — xem Flyway V63__remove_dss_demo_electronics_catalog.sql.
-- Catalog / seller thật vẫn nằm ở các migration seed khác (V24, V27, …).
--
-- File này giữ lại để không gãy script tham chiếu; không seed lại SP/shop DSS điện tử.

SELECT 1;
